# 롤대남 단두대 — PRD (Product Requirements Document)

> **버전** v0.3 (초안) · **작성일** 2026-06-07 · **상태** 컨셉 확정 / 일부 세부 미정

> **v0.3 변경점**: 닉네임 4자리 PIN 잠금(선택) 기능을 정식 요구사항으로 승격
> **v0.2 변경점**: 사진 서버 저장 허용 · 닉네임 기반 기록 이어쓰기 · 랭킹창(시간 대비 격파 수) · 감도 조정 추가

---

## 1. 개요

| 항목 | 내용 |
|------|------|
| 제품명 | **롤대남 단두대** |
| 한 줄 정의 | 친구 얼굴을 타겟으로 띄워 단두대로 "처형"하는 웹 기반 에임·반응속도 트레이너 |
| 핵심 컨셉 | 닉네임(+PIN) 입장 → 친구 얼굴 클릭으로 격파 → 스코어 누적 저장 → 친구끼리 랭킹 경쟁 |
| 플랫폼 | 웹 (데스크톱 마우스 / 모바일 터치) |
| 톤 | 친구끼리 셀프디스·밈 기반 장난 (※ 용도 미정에 따라 조정) |

### 1.1 목적
단순 에임 연습 툴이 아니라, **반응속도·손풀기 훈련을 게임화**한다. 타겟을 "친구 얼굴"로 만들어 재미와 경쟁 동기를 극대화하고, 닉네임 기반 누적 스코어로 친구 간 경쟁 루프를 만든다.

### 1.2 디자인 철학 (가장 중요)
> **"실력 향상에 집중하는 게임"** — 이펙트가 플레이를 방해하면 안 된다.

화려함이 아니라 **빠르고 깔끔한 타격감**이 목표다. 모든 연출은 입력 흐름을 끊지 않는 선에서 최소·고속으로 처리한다.

---

## 2. 핵심 게임플레이

### 2.1 메인 모드 — 플릭샷 (타겟 격파)
- 제한시간(예: 30초) 동안 친구 얼굴 타겟이 화면에 등장
- 플레이어가 빠르게 조준 → 클릭(터치)으로 격파
- 격파 즉시 다음 타겟 등장, 반복
- 측정값: 격파 수, **반응속도(ms)**, 정확도(%), 콤보

### 2.2 타격 연출 시퀀스 (전체 ≈ 0.2초)
1. **칼날 낙하** — 얼굴 클릭(명중) 시 칼날이 위에서 순간 낙하 (≈80ms, 가속 곡선)
2. **얼굴 분할** — 착지와 동시에 얼굴이 정중앙에서 반으로 갈라짐 (≈100ms)
3. **소멸** — 갈라진 두 조각이 좌우로 튕기며 페이드아웃, 즉시 다음 타겟

> 모션 블러·파티클·화면 흔들림은 제거 또는 최소화. 점수/콤보 숫자만 짧게 팝업.

### 2.3 설정 — 감도 조정 (Sensitivity)
- 플레이어가 **조준 감도를 직접 조절**할 수 있는 설정 제공 (에임 게임 핵심 기능)
- 슬라이더 + 수치 입력 (예: 0.1 ~ 5.0 배율, 기본값 1.0)
- 설정값은 닉네임 프로필에 함께 저장되어 재입장 시 유지

### 2.4 확장 모드 (미정) — 트래킹
떠다니는 얼굴 안에 커서를 유지, 머문 시간 비율 × 배율로 점수 누적 → **포함 여부 TBD**.

---

## 3. 사용자 플로우 · 계정(닉네임+PIN)

```
[닉네임 입력] → [PIN 입력/설정] → [감도·모드 설정] → [게임 플레이] → [결과 카드] → [랭킹 등록·갱신]
```

### 3.1 닉네임 + 4자리 PIN (선택 잠금)
- 회원가입 없음. 닉네임 한 줄로 진입하되, **4자리 PIN으로 내 기록을 잠글 수 있음(선택)**
- **신규 닉네임**: 입장 시 PIN 설정 여부 선택 → 설정하면 4자리 입력(확인 1회)
- **기존 닉네임**:
  - PIN이 걸려 있으면 → **올바른 PIN 입력해야 기록 이어쓰기 가능** (틀리면 진입 거부)
  - PIN이 없으면 → 기존처럼 바로 이어쓰기
- PIN은 **해시로 저장**(평문 저장 금지), 입력 실패 횟수 제한 권장
- (선택) PIN 분실 대비 정책은 추후 결정 — 분실 시 새 닉네임 사용 안내 등

### 3.2 기록 이어쓰기
- 같은 닉네임(+PIN 통과)으로 재입장 시 **이전 누적 기록을 불러와 이어서 저장**
- 누적 항목: 총 플레이 횟수, 베스트 스코어, 베스트 반응속도, 최고 등급, 누적 격파 수, 감도 설정
- **결과 카드**: 이번 판 점수 · 평균 반응속도(ms) · 정확도(%) · 등급 + 개인 베스트 갱신 여부
- **등급 체계(안)**: S+ / S / A / B / C — 반응속도·정확도·격파 수 가중 합산 (공식 세부 TBD)

---

## 4. 스코어 · 랭킹 시스템

### 4.1 랭킹창
- 별도 **랭킹 화면** 제공
- 핵심 표시 지표: **"몇 초 동안 몇 번 격파했는지"** (예: 30초 / 42격파) + 분당 격파 수(targets/min)
- 랭킹 테이블 컬럼(안):

| 순위 | 닉네임 | 격파 수 | 플레이 시간 | 평균 반응속도(ms) | 정확도 | 등급 |
|------|--------|---------|-------------|--------------------|--------|------|

- 정렬 기준: 기본은 격파 수(또는 종합 점수) 내림차순, 동점 시 반응속도 빠른 순
- 갱신: 게임 종료 시 자동 등록, 같은 닉네임은 **베스트 기록만 랭킹에 반영**(개인 누적은 별도 저장)

### 4.2 경쟁 장치 (후보)
- 링크 공유 도전 / 일간·주간 리더보드 / 결과 카드 이미지 공유 / 방 코드 그룹 리그
- **경쟁 범위: 전체 공개 랭킹 기본 + 방 코드 그룹은 확장 (세부 TBD)**

---

## 5. 에셋

| 에셋 | 내용 | 상태 |
|------|------|------|
| 배경 | 사용자(대표)가 직접 캡처한 이미지 사용 | 추후 제공 |
| 타겟 얼굴 | 친구 얼굴 사진 (직접 업로드) | 서버 저장 |
| 단두대 칼날 | 직접 제작 (SVG/이미지) | 제작 예정 |
| 효과음 | 칼날 낙하음 · 명중음 · 미스음 | 확장 (선택) |

---

## 6. 기술 스택 (안)

- **프론트엔드**: 단일 페이지 (React 또는 vanilla JS + Canvas). 반응속도 측정 정확도를 위해 입력 지연 최소화
- **백엔드/저장**: **Supabase**
  - **DB(Postgres)**: 닉네임 프로필 + 스코어 기록
    - `profiles`: `nickname`(PK), `pin_hash`(nullable, 해시), `sensitivity`, `total_plays`, `best_score`, `best_reaction_ms`, `total_kills`, `updated_at`
    - `scores`: `id`, `nickname`(FK), `mode`, `kills`, `duration_sec`, `reaction_ms`, `accuracy`, `grade`, `room_code`, `created_at`
  - **Storage**: 친구 얼굴 사진을 **서버에 저장** (버킷 + 닉네임/세션 단위 폴더)
- **PIN 인증 로직**:
  - 신규: PIN 설정 시 해시 후 `pin_hash` 저장
  - 기존: `pin_hash`가 있으면 입력 PIN 해시 비교 → 일치해야 진입, 불일치 시 거부(실패 횟수 제한)
  - 보안상 PIN 검증은 서버측(예: Supabase RPC/Edge Function)에서 처리 권장
- **기록 이어쓰기 로직**: 입장 시 `nickname`으로 `profiles` 조회 → (PIN 통과 시) 누적값·감도 로드, 없으면 신규 생성

---

## 7. 개발 범위

### MVP
- 닉네임 진입 + **4자리 PIN 잠금(선택)** + 기록 이어쓰기 (Supabase profiles)
- 플릭샷 모드 (타겟 등장·격파)
- 단두대 타격 연출 (칼날 낙하 → 얼굴 분할 → 소멸)
- 감도 조정 설정
- 점수 / 반응속도 / 정확도 / 콤보 측정
- 결과 화면 + 등급
- 친구 얼굴 사진 업로드 (Supabase Storage)
- 랭킹창 (시간 대비 격파 수)

### 확장
- 방 코드 기반 친구 그룹 리그
- 트래킹 모드
- 결과 카드 이미지 공유
- 효과음
- PIN 분실 복구 정책

---

## 8. 비기능 요구사항
- 반응속도 측정 정확도 (입력~렌더 지연 최소화)
- 60fps 안정 동작
- 데스크톱(마우스)·모바일(터치) 동시 지원
- 사진 업로드 용량·포맷 제한 및 저장 보안
- PIN 해시 저장 및 입력 실패 횟수 제한

---

## 9. 오픈 이슈 (TBD)
1. **경쟁 범위** — 전체 공개 랭킹(기본) 외 방 코드 그룹 리그 포함 시점
2. **주 용도·톤** — 순수 친구 장난용 / 모두의러닝 행사 아이스브레이커 겸용
3. **트래킹 모드 포함 여부**
4. **점수·등급 산정 공식** 세부 튜닝
5. **PIN 분실 복구 정책** — 분실 시 처리 방식
6. **사진 프라이버시·삭제 정책** — 서버 저장 시 보관 기간, 삭제 요청 처리
7. **배경 IP 검토** — 캡처 배경을 공개 배포·공유까지 확장할 경우 원작 저작권 검토 필요
